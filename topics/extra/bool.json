{
    "coq" : "Require Import Bool.\nNotation Bool := bool.\nNotation \"#cases B #if A #otherwise C #endcases\" := (if A then B else C) (at level 2).\nNotation \"( A && B ) = C \" := (A && B = C) (at level 30, only printing).\n"
}
